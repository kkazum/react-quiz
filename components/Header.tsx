import Head from 'next/head';

const Header = () => {
  return (
    <Head>
      <title>React Quiz</title>
      <meta name="description" content="React Quiz by create next app" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
  );
};

export default Header;
